## gif_space

This is configured to run locally, you might want to npm install forever to run it on the amazon cloud. 

All of the code to get gif_space up and running.

Server:
Runs on node.js, using express and socket.io.
Uses mongodb for the database.

Client:
jquery and stuff.

How to run?

npm install -d
node app.js

then you would access it by entering the following if your web browser:
http://localhost:8080
